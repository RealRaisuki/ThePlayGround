// App constants and configuration
class AppConstants {
  static const String appName = 'Smart To-Do List';
  static const String storageKey = 'tasks';

  // Animation durations
  static const Duration listAnimationDuration = Duration(milliseconds: 375);
  static const Duration statsAnimationDuration = Duration(milliseconds: 500);

  // Date ranges
  static final DateTime firstDate = DateTime(2000);
  static final DateTime lastDate = DateTime(2100);
}
